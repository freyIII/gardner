import { ObjectFindPipe } from './object-find.pipe';

describe('ObjectFindPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectFindPipe();
    expect(pipe).toBeTruthy();
  });
});
